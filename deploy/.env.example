# 服务器部署环境变量
# 用法：在服务器 /opt/mars-site/ 下复制为 .env（与 docker-compose.yml 同级），再执行
#   docker compose up -d --build

# ---- 百度统计（可选）----
# 1. 登录 https://tongji.baidu.com/ → 管理 → 网站列表 → 获取代码
# 2. 代码里形如：hm.src = "https://hm.baidu.com/hm.js?abc123def456...";
#    问号后面那串 32 位字符就是统计 ID，填到下面
# 3. 填好后必须重新 build —— NEXT_PUBLIC_* 是构建时内联的，改了不重建不生效
# 留空则完全不加载统计脚本。
# 当前已配置：marsmz.top 主站统计 ID
NEXT_PUBLIC_BAIDU_ANALYTICS_ID=b20d77ed3d94cfe964b20eeb5c99667e

# ---- 部署注意事项 ----
# 1. 阿里云安全组需放行 80 / 443（用 deploy.sh 远程部署还需放行 22）
# 2. 域名 A 记录解析到服务器公网 IP
# 3. 国内服务器需 ICP 备案通过后 80/443 才能正常访问
# 4. 本站复用服务器现有 nginx 反代 + acme.sh 证书（见 deploy/marsmz.top.conf）

# 服务器上执行：
#   docker compose up -d --build
#   docker compose logs -f
