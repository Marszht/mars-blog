# 部署注意事项
# 1. 阿里云安全组必须放行 80 / 443 端口
# 2. 域名 A 记录解析到服务器公网 IP
# 3. 国内服务器需 ICP 备案通过后 80/443 才能正常访问
# 4. 证书由 Caddy 自动申请（Let's Encrypt，免费），到期自动续期

# ---- 百度统计（可选）----
# 在百度统计创建站点，拿到 HM 代码里的 ID，填到这里后重新 build：
# NEXT_PUBLIC_BAIDU_ANALYTICS_ID=你的统计ID

# 服务器上执行：
#   docker compose up -d --build
#   docker compose logs -f
